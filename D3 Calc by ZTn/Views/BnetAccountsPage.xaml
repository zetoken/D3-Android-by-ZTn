<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ZTn.Pcl.D3Calculator.Views;assembly=ZTn.Pcl.D3Calculator"
             x:Class="ZTn.Pcl.D3Calculator.Views.BnetAccountsPage"
             Title="D3 Calc by ZTn">
  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Add Account" Icon="ic_menu_add.png" Order="Primary" Clicked="OnAddAccount" />
  </ContentPage.ToolbarItems>
  <ContentPage.Content>
    <ListView ItemsSource="{x:Static local:BnetAccountsPage.Accounts}" HasUnevenRows="True" ItemSelected="OnSelection" IsPullToRefreshEnabled="true" Refreshing="OnRefresh">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <ViewCell.ContextActions>
              <MenuItem Text="Edit" Icon="ic_menu_edit.png" CommandParameter="{Binding .}" Clicked="OnEdit" />
              <MenuItem Text="Delete" Icon="ic_menu_delete.png" IsDestructive="true" CommandParameter="{Binding .}" Clicked="OnDelete" />
            </ViewCell.ContextActions>
            <StackLayout Padding="10,5">
              <Label Text="{Binding BattleTag}" TextColor="Accent" FontSize="Large" />
              <Label Text="{Binding Host}" FontAttributes="Italic" />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>