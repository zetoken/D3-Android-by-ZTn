<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ZTn.Pcl.D3Calculator.Views;assembly=ZTn.Pcl.D3Calculator"
             x:Class="ZTn.Pcl.D3Calculator.Views.HeroPage">

  <TabbedPage.ToolbarItems>
    <ToolbarItem Text="Refresh" Icon="" Order="Primary" Clicked="OnRefresh" />
  </TabbedPage.ToolbarItems>

  <TabbedPage.Children>
    <views:HeroBlizzardPage BindingContext="{Binding .}" />
    <!--views:HeroComputedPage BindingContext="{Binding .}" /-->
    <!--views:HeroSkillsPage BindingContext="{Binding .}" /-->
    <views:HeroItemsPage BindingContext="{Binding .}" />
  </TabbedPage.Children>

</TabbedPage>